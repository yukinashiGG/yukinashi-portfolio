# ユキナシ ホームページ — 引継ぎドキュメント

## プロジェクト概要

Live2Dイラストレーター「ユキナシ」の公式ポートフォリオ＆料金表サイト。
単一HTMLファイル構成で、GitHub Pagesでホスティング。

| 項目 | 値 |
|------|-----|
| **公開URL** | https://yukinashigg.github.io/yukinashi-homepage/ |
| **GitHub** | https://github.com/yukinashiGG/yukinashi-homepage |
| **ローカルパス** | `c:\Users\shuta\c_local_date\ユキナシHP\` |
| **デプロイ方法** | `main` ブランチに push → GitHub Pages 自動デプロイ |

---

## ファイル構成

```
ユキナシHP/
├── index.html          ← メインサイト（HTML/CSS/JS全部入り、約1170行）
├── terms.html          ← 利用規約ページ
├── avatar.png          ← プロフィール画像
├── build_gallery.py    ← ギャラリー自動ビルドスクリプト（.gitignored）
├── .gitignore
└── gallery/
    ├── _Source/         ← 元画像（.gitignored、ローカルのみ）
    │   ├── 01_VTuberモデル用イラスト/
    │   │   ├── 01_不知火燈様[...]/
    │   │   │   ├── サムネ.png   ← サムネ用（必須）
    │   │   │   ├── 00_立ち絵.png
    │   │   │   └── ...
    │   │   └── ...
    │   └── 02_オリジナルイラスト/
    │       └── ...
    └── web/             ← ビルド後のwebp画像（pushされる）
```

---

## ギャラリー自動ビルドシステム

### 仕組み
`build_gallery.py` が `_Source/` フォルダを走査し、以下を自動実行：
1. 各画像を **webp に圧縮**（長辺最大2000px、品質85%）
2. `サムネ.png` → `thumb.webp` にリネーム
3. `index.html` 内のギャラリーHTMLを**自動書き換え**

### 運用フロー（ユーザーが普段やること）
```
1. gallery/_Source/ にフォルダと画像を追加・編集
2. 「更新した」と言うだけ
3. → build_gallery.py 実行 → git add/commit/push → デプロイ完了
```

### フォルダ命名規則
```
[番号]_[カテゴリ]/[番号]_[表示名][ 説明テキスト]/
  例: 01_VTuberモデル用イラスト/01_不知火燈様[ VTuber向け キャラクターデザイン・イラスト制作]/
```
- **番号** → 表示順の制御
- **[ ]内** → カードのサブタイトルとして表示
- **サムネ.png** → 各フォルダに必須（カードのサムネイル画像）

### ビルドコマンド
```bash
cd c:\Users\shuta\c_local_date\ユキナシHP
python build_gallery.py; git add -A; git commit -m "Update gallery"; git push origin main
```

---

## 現在のデザイン仕様

### ヒーロー（ファーストビュー）
- **背景**: 白ベース + 薄ピンクグラデーション（`#fdf2f8` → `#ffffff`）
- **パーティクル**: 薄ピンク（`rgba(236,72,153,0.3)`）、4px、上方向に浮遊アニメーション
- **テキスト色**: ダーク（`#1a1040`）、グラデーション強調テキストあり
- **ボタン**: プライマリ=ピンク→パープルグラデ、セカンダリ=白+ボーダー

### ナビバー
- **背景**: 白（`white`）、下ボーダー（`#f0e0ea`）
- **ロゴ**: `Yukinashi`（Outfit フォント、太字、`#1a1040`）
- **リンク色**: `#777` → ホバー `#1a1040`
- **スクロール時**: 白背景 + 軽いシャドウ追加
- **固定表示**（`position: fixed`）

### ギャラリーカード
- **ホバー時**: ピンク→パープルのグラデーション出現 + カード浮き上がり
- **ライトボックス**: クリックで全画面表示、左右矢印でスライド、ESCで閉じ
- **ズーム機能**: 削除済み（使い勝手が悪かったため）

### カラーパレット
```
--accent-pink: #ec4899
--accent-pink-light: #f472b6
--accent-purple: #8b5cf6
--gradient-main: linear-gradient(135deg, #ec4899, #a855f7)
--text-primary: #1a1a2e
--bg-primary: #fefcfd
```

---

## サイトセクション構成（上から順）

1. **ナビバー** — 固定、Yukinashiロゴ + セクションリンク
2. **ヒーロー** — 背景デモリール動画（15MB, autoplay muted loop）+ 白オーバーレイ75% + キャッチコピー + CTAボタン3つ
3. **受付状況バー** — 現在の空き状況（月1回更新）
4. **実績・信頼バッジ** — カバー株式会社、映像制作、継続案件、NDA対応
5. **自分について** — プロフィール・経歴（映像6年→ゲーム4年→フリー2年）
6. **制作実績（showcase）** — 企業3件+個人6件、サムネ画像+クリックでライトボックス動画再生（音声ON）
7. **ギャラリー** — カテゴリタブ（VTuber用 / オリジナル）+ カード一覧
8. **~~デモリール~~** — 現在コメントアウト（後日YouTube埋め込みで追加予定）
9. **6つの強み** — 一枚絵対応、映像ワンストップ、表情ラフ作成等
10. **料金表** — 3プラン（MODELING/STANDARD SET/FULL SET）+ 個別サービス
11. **オプション料金** — 表情差分、パーツ差分、複雑構造加算、モーション、権利・納期
12. **制作タイムライン** — 幅持ち納期表記（約4〜6週間 / 約6〜8週間 / 約3ヶ月〜）
13. **FAQ** — アコーディオン形式
14. **お問い合わせ** — Google Form埋め込み + DM・メールリンク
15. **フッター** — SNSリンク + 利用規約リンク

---

## 外部連携

| サービス | 用途 | URL |
|---------|------|-----|
| **Google Form** | 依頼・お問い合わせフォーム | index.html内のリンクから遷移 |
| **Google Apps Script** | フォーム自動返信 | FormAutoReply_Final（yukinashi.contact@gmail.com） |
| **GitHub Pages** | ホスティング | mainブランチ自動デプロイ |
| **Google Analytics** | アクセス解析 | GA4 `G-9TESHKNJZ6` |

---

## .gitignore で除外されているもの

- `gallery/_Source/` — 元画像（大容量のため）
- `*.psd` — Photoshopファイル
- `build_gallery.py` — ビルドスクリプト（サイトに不要）
- `videos/*/情報.txt` — 動画の情報テキスト
- `.agents/` — エージェント設定
- `__pycache__/` — Python キャッシュ

---

## 動画ファイル構成

```
videos/
├── デモリール/
│   └── デモリール背景用.mp4     ← ヒーロー背景（15MB, 720p, 2Mbps）
├── 企業様_01/ (カバー株式会社)
│   ├── corporate_01.mp4          ← ライトボックス再生用（83MB）
│   └── thumb/thumb_00000.jpg     ← カード用サムネイル（121KB）
├── 企業様_02/ (コロニーラボ)
│   ├── corporate_02.mp4
│   └── thumb/thumb_00000.jpg
├── 企業様_03/ (にとりずむ)
│   ├── A03_にとりずむ_シエラ.mp4
│   └── thumb/thumb_00000.jpg
├── 個人様_01〜06/
│   ├── [各動画].mp4
│   └── thumb/thumb_00000.jpg
```

### パフォーマンス最適化
- カード表示: autoplay動画(615MB) → **サムネ画像(約900KB)**に変更済み
- クリック時のみ動画読み込み（ライトボックス）
- ヒーロー背景: **67MB → 15MB** に圧縮済み
- 初回ページ読み込み: 約**16MB**（背景動画15MB + サムネ等1MB）

---

## 残タスク・今後の検討事項

### 未対応
- [ ] **デモリールセクション**: YouTube埋め込みで追加（動画制作後）
- [ ] **実績動画の圧縮**: 各50〜83MBのクリック再生用動画を5〜10MBに再書き出し推奨
- [ ] **ステータスウィジェット有効化**: GAS移行後、`autoreply_widget.py` の `GAS_URL` を設定
- [ ] **独自フォーム実装**: Google Form → HP内カスタムフォームへ移行（入力内容確認画面付き）
- [ ] **受付状況の更新**: 月1回、ヒーローの月枠を更新

### 完了済み
- [x] **logo_preview.html の削除**: コミット `a99b022` で削除済み
- [x] **ナビバーロゴ左端配置**: コミット `d3f4c3b` で対応済み
- [x] **実績動画の制作仕様書**: `_archive/hero_video_spec.md` 作成済み
- [x] **GASプロジェクト移行**: 送信元を `yukinashi.contact@gmail.com` に変更完了
- [x] **古いGoogle Formsアーカイブ**: Google Drive「old」フォルダに2件移動済み
- [x] **Google Analytics**: GA4設置（`G-9TESHKNJZ6`）、フォーム/DM/メールクリック計測付き
- [x] **ヒーロー背景動画**: デモリール背景用.mp4（15MB）埋め込み済み
- [x] **実績カード動画対応**: 企業3件+個人6件、サムネ+ライトボックス動画再生
- [x] **サムネイル最適化**: autoplay → 静止画サムネ+▶アイコン（615MB→900KB）
- [x] **表情ラベル統一**: HP（Bランク/Aランク→通常/特殊）+ フォーム（A/B/C→通常/特殊）
- [x] **料金更新（4/14）**: 納期を幅持ち表記に変更、複雑構造加算カテゴリ新設、モーション「デフォルト含まず」明記
- [x] **受付状況更新（4/14）**: 4月→満枠、5月→残り1枠、6月→受付中
- [x] **利用規約更新（4/12）**: R-18G禁止・AI学習禁止を追加
- [x] **不知火燈動画更新（4/14）**: 個人様_02の動画を差し替え

### 参照ファイル
- `_archive/hero_video_spec.md` — ヒーロー動画のぼかし・オーバーレイ設定値
- `_archive/hero_video_test.html` — 動画背景テストページ
- `demo_reel_構成.md` — デモリール1分構成案
- `c:\Users\shuta\c_local_date\業務データ\ユキナシ_料金条件まとめ.md` — 料金・条件の確定版まとめ
- `c:\Users\shuta\c_local_date\業務データ\ユキナシ_ホームページ_業務情報.md` — 業務情報全般
- `c:\Users\shuta\c_local_date\業務データ\Google_ブラウザ操作手順.md` — Google Forms/GASのブラウザ操作手順

### 運用上の注意
- `_Source/` フォルダはGitに含まれないので、**ローカルでのみ管理**
- ビルドスクリプトには **Python + Pillow** が必要（`pip install Pillow`）
- 画像を追加/変更したら「更新した」と言えばビルド→プッシュを実行
- 動画ファイルはGitHub推奨上限(50MB)を超えているが、100MB以下なのでpush可能

---

## デザイン変遷メモ（判断に至った経緯）

1. **ヒーロー背景**: ダーク紺 → **白ベース + 薄ピンク** → **デモリール動画+白オーバーレイ75%**
2. **ナビバー**: ダーク紺 → **白**に変更（ヒーロー変更に合わせて）
3. **ロゴテキスト**: スプリットカラー（Yuki白/nashiピンク）→ **全白**に戻す（ナルシスト感回避）
4. **ズーム機能**: 実装したが「上までスクロールできない」問題 → **削除**（UX改善のため）
5. **ライトボックス画像サイズ**: 70vw/80vh → **85vw/85vh** に拡大（キャラが小さく見える問題）
6. **実績カード**: autoplay動画 → **サムネ画像+▶アイコン**（ページ読み込み615MB→1MB）
7. **表情ラベル**: A/B/Cランク → **通常/特殊**（HPとフォームの用語統一）
8. **納期表記**: 固定（約1.5ヶ月等）→ **幅持ち表記**（約4〜6週間等）— バッファ確保
9. **利用規約**: R-18のみ → **R-18G・AI学習禁止も追加**（時流に対応）
10. **オプション**: 複雑構造加算カテゴリ新設（スカート・翼・胸揺れ等を明示化）
